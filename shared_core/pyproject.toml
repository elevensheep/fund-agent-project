[project]
name = "shared-core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "elevensheep", email = "fttc100@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "structlog>=26.1.0",
    "pyyaml>=6.0",
]

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
