apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://agent_prometheus:9090
    access: proxy
    isDefault: true

  - name: Loki
    type: loki
    url: http://agent_loki:3100
    access: proxy
    isDefault: false
